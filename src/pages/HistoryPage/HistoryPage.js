import styled from "styled-components";
import { Container } from "../../assets/styles/GlobalStyle";

export default function HistoryPage({ }) {
    return(
        <Container>
        Bem vindo a HistoryPage
        </Container>
    )
}