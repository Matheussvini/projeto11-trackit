import styled from "styled-components";
import { Container } from "../../assets/styles/GlobalStyle";

export default function TodayPage({ }) {
    return(
        <Container>
        Bem vindo a TodayPage
        </Container>
    )
}